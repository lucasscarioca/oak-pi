---
name: ship
description: Commit and push the current work on the current branch. Use only when the user explicitly invokes "$ship" or directly asks to use the ship skill; do not use proactively for general implementation or validation tasks.
---

Commit and push the current work on the current branch.

Do the following:
- Make sure the changes are ready to ship.
- Create a clear commit message that follows Conventional Commits, including scope when it is obvious.
- Commit the changes.
- Push the current branch.

